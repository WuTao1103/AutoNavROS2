# AutoNavROS2 🤖

一个全面的室内自主TurtleBot导航系统，使用SLAM和路径规划算法，无需GPS。该项目结合了ROS2后端、实时Web可视化和基于MQTT的远程控制功能。

<div align="center">

![ROS2](https://img.shields.io/badge/ROS2-Jazzy-blue.svg)
![Python](https://img.shields.io/badge/Python-3.8+-green.svg)
![TypeScript](https://img.shields.io/badge/TypeScript-5.8+-blue.svg)
![React](https://img.shields.io/badge/React-19.2-61DAFB.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

</div>

## 📋 目录

- [🌟 功能特性](#-功能特性)
- [🏗️ 系统架构](#️-系统架构)
- [🚀 快速开始](#-快速开始)
- [🎮 Web仪表盘](#-web仪表盘)
- [📡 MQTT控制接口](#-mqtt控制接口)
- [📦 项目结构](#-项目结构)
- [⚙️ 配置参数](#️-配置参数)
- [🛠️ 开发指南](#️-开发指南)
- [📖 相关文档](#-相关文档)

## 🌟 功能特性

### 核心导航系统
- **🗺️ 实时SLAM建图**: 使用LiDAR数据进行占用栅格建图
- **🧭 路径规划**: A*算法，支持障碍物膨胀和路径平滑
- **🎯 目标设置**: 通过RViz或Web界面进行交互式目标设置
- **🚗 纯跟踪控制**: 平滑的路径跟随，可配置控制参数
- **🛡️ 安全系统**: 碰撞检测和紧急停止功能

### Web可视化仪表盘
- **📊 实时地图可视化**: 交互式2D占用栅格地图显示
- **🤖 机器人状态监控**: 实时位姿、速度和电池状态
- **📈 传感器数据显示**: LiDAR点云和路径可视化
- **🎮 手动控制**: 类摇杆的机器人控制界面
- **📝 系统日志**: 实时ROS节点状态和调试信息

### 远程控制与集成
- **📡 MQTT集成**: 通过MQTT代理进行远程控制
- **🌐 Web API**: 用于外部系统集成的RESTful API
- **📱 多平台访问**: 基于Web的界面，可从任何设备访问
- **🔄 实时同步**: ROS2和Web界面之间的实时数据流

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                     Web仪表盘                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │地图渲染器   │  │控制面板     │  │  状态与日志         │ │
│  │• 2D地图     │  │• 手动控制   │  │  • 机器人状态       │ │
│  │• 机器人位姿 │  │• 目标设置   │  │  • 系统日志         │ │
│  │• 激光数据   │  │• 紧急停止   │  │  • 传感器数据       │ │
│  └─────────────┘  └─────────────┘  └─────────────────────┘ │
└─────────────────┬───────────────────────────────────────────┘
                  │ WebSocket/HTTP
                  ▼
         ┌─────────────────┐
         │   MQTT代理      │
         │ (Eclipse        │
         │  Mosquitto)     │
         └─────────┬───────┘
                   │ MQTT话题:
                   │ • /robot/command/*
                   │ • /robot/status/*
                   │ • /robot/sensor/*
                   ▼
    ┌─────────────────────────────────────┐
    │         ROS2后端系统                │
    │  ┌─────────────┐  ┌───────────────┐ │
    │  │MQTT-ROS     │  │  SLAM系统     │ │
    │  │桥接器       │  │  • 建图       │ │
    │  │• 话题       │◄─┤  • 定位       │ │
    │  │  转换       │  │               │ │
    │  │• 数据同步   │  └───────────────┘ │
    │  └─────────────┘                    │
    │  ┌─────────────┐  ┌───────────────┐ │
    │  │路径规划     │  │  控制器       │ │
    │  │• A*规划器   │  │  • 纯跟踪     │ │
    │  │• 目标桥接   │  │  • 安全控制   │ │
    │  │             │  │               │ │
    │  └─────────────┘  └───────────────┘ │
    └─────────────┬───────────────────────┘
                  │ ROS2话题与服务:
                  │ • /scan, /cmd_vel, /odom
                  │ • /map, /path, /goal_pose
                  ▼
         ┌─────────────────┐
         │   TurtleBot3    │
         │ • LiDAR传感器   │
         │ • IMU/里程计    │
         │ • 底盘控制      │
         └─────────────────┘
```

### 三层开发方法

1. **Level 1 - 基础层** ✅
   - 使用LiDAR的基础避障
   - 安全的手动导航
   - ROS2基础设施搭建

2. **Level 2 - 自主导航** ✅
   - 基于SLAM的建图和定位
   - A*路径规划与避障
   - 纯跟踪路径跟随

3. **Level 3 - 高级界面** ✅
   - 基于Web的可视化仪表盘
   - MQTT远程控制集成
   - 实时监控和日志记录

## 🚀 快速开始

### 系统要求

- **ROS2 Jazzy** 或更高版本
- **TurtleBot3仿真包**
- **Node.js 18+** (用于仪表盘)
- **Python 3.8+**
- **Gazebo** (用于仿真)

### 安装步骤

1. **克隆仓库:**
   ```bash
   git clone <repository-url>
   cd AutoNavROS2
   ```

2. **构建ROS2工作空间:**
   ```bash
   colcon build
   source install/setup.bash
   ```

3. **安装仪表盘依赖:**
   ```bash
   cd dashboard
   npm install
   cd ..
   ```

### 启动系统

#### 方式一: 交互式菜单 (推荐)
```bash
./quick_start.sh
```
交互式菜单提供引导式系统管理功能：
- 系统状态监控
- 自动依赖检查
- 分步启动流程
- 内置故障排除工具

#### 方式二: 自动化脚本
```bash
# 使用tmux启动完整系统
./scripts/start_full_system.sh

# 单独启动Web仪表盘
cd dashboard && npm run dev

# 启动RViz可视化 (可选)
./scripts/start_rviz_slam.sh
```

#### 方式三: 手动启动 (高级用户)
```bash
# 终端1: 启动Gazebo仿真
export TURTLEBOT3_MODEL=burger
ros2 launch turtlebot3_gazebo turtlebot3_world.launch.py

# 终端2: 启动SLAM + 路径规划
source install/setup.bash
ros2 launch slam slam_with_planning.launch.py use_gazebo:=true

# 终端3: 启动Web仪表盘
cd dashboard && npm run dev

# 终端4: 启动RViz (可选)
./scripts/start_rviz_slam.sh
```

## 🎮 Web仪表盘

Web仪表盘为监控和控制机器人系统提供了全面的界面。

### 功能特性

- **🗺️ 交互式地图**: 实时占用栅格，点击导航
- **📊 状态监控**: 机器人位姿、速度、电池和连接状态
- **🕹️ 手动控制**: 虚拟摇杆进行机器人手动操作
- **📈 传感器可视化**: 实时LiDAR点云显示
- **🎯 目标设置**: 在地图上点击设置导航目标
- **📜 系统日志**: 实时ROS节点消息和调试信息
- **🚨 紧急控制**: 立即停止和安全覆盖

### 访问方式

```bash
cd dashboard
npm run dev
# 在浏览器中打开 http://localhost:5173
```

### 仪表盘组件

#### MapVisualizer (地图可视化器)
- 基于Canvas的2D地图渲染
- 实时机器人位姿和方向
- LiDAR扫描点云可视化
- 通过鼠标点击进行交互式目标设置
- 路径可视化与路径点

#### ControlPanel (控制面板)
- 手动速度控制 (线速度/角速度)
- 紧急停止功能
- 模式切换 (空闲/导航/紧急停止)
- 快捷操作按钮

#### StatusWidget (状态组件)
- 实时机器人状态显示
- 电池状态和电压
- 连接状态指示器
- 当前操作模式

#### LogConsole (日志控制台)
- ROS节点的实时系统日志
- 按日志级别过滤 (INFO/WARN/ERROR)
- 带节点名称的时间戳消息
- 自动滚动和缓冲区管理

## 📡 MQTT控制接口

### 架构设计

系统支持基于MQTT的远程控制，用于IoT集成和外部系统通信。

```
MQTT代理 (Eclipse Mosquitto)
├── /robot/command/velocity     # 速度命令
├── /robot/command/goal         # 目标位置设置
├── /robot/command/emergency    # 紧急停止
├── /robot/status/pose          # 机器人位置反馈
├── /robot/status/battery       # 电池状态
├── /robot/status/mode          # 操作模式
└── /robot/sensor/lidar         # LiDAR数据流
```

### MQTT话题

#### 命令话题 (订阅)
- **`/robot/command/velocity`**: 发送速度命令
  ```json
  {"linear": 0.5, "angular": 0.0}
  ```

- **`/robot/command/goal`**: 设置导航目标
  ```json
  {"x": 2.0, "y": 1.5, "theta": 0.0}
  ```

- **`/robot/command/emergency`**: 紧急停止
  ```json
  {"stop": true}
  ```

#### 状态话题 (发布)
- **`/robot/status/pose`**: 当前机器人位置
  ```json
  {"x": 1.2, "y": 0.8, "theta": 1.57, "timestamp": "2024-12-04T10:30:00Z"}
  ```

- **`/robot/status/battery`**: 电池信息
  ```json
  {"percentage": 85, "voltage": 12.4, "status": "Discharging"}
  ```

- **`/robot/status/mode`**: 操作模式
  ```json
  {"mode": "NAVIGATING", "timestamp": "2024-12-04T10:30:00Z"}
  ```

### 设置MQTT集成

1. **安装MQTT代理:**
   ```bash
   sudo apt install mosquitto mosquitto-clients
   sudo systemctl start mosquitto
   ```

2. **配置MQTT桥接:**
   ```bash
   # 编辑mqtt_bridge配置
   nano src/mqtt_bridge/config/mqtt_config.yaml
   ```

3. **启动MQTT支持:**
   ```bash
   ros2 launch slam slam_with_planning.launch.py use_mqtt:=true
   ```

### 远程控制示例

#### Python客户端
```python
import paho.mqtt.client as mqtt
import json

client = mqtt.Client()
client.connect("localhost", 1883, 60)

# 发送速度命令
velocity_cmd = {"linear": 0.5, "angular": 0.2}
client.publish("/robot/command/velocity", json.dumps(velocity_cmd))

# 设置导航目标
goal = {"x": 3.0, "y": 2.0, "theta": 0.0}
client.publish("/robot/command/goal", json.dumps(goal))
```

#### Node.js客户端
```javascript
const mqtt = require('mqtt');
const client = mqtt.connect('mqtt://localhost:1883');

// 订阅机器人状态
client.subscribe('/robot/status/pose');
client.on('message', (topic, message) => {
  const data = JSON.parse(message.toString());
  console.log(`机器人位置: x=${data.x}, y=${data.y}`);
});

// 发送命令
client.publish('/robot/command/velocity',
  JSON.stringify({linear: 0.3, angular: 0.0}));
```

## 📦 项目结构

```
AutoNavROS2/
├── README.md                    # 本文件
├── dashboard/                   # Web可视化界面
│   ├── App.tsx                 # React主应用
│   ├── components/             # React组件
│   │   ├── MapVisualizer.tsx   # 2D地图渲染器
│   │   ├── ControlPanel.tsx    # 机器人控制界面
│   │   ├── StatusWidget.tsx    # 状态显示
│   │   └── LogConsole.tsx      # 系统日志
│   ├── services/               # 业务逻辑
│   │   └── rosSimulation.ts    # ROS仿真服务
│   ├── types.ts                # TypeScript类型定义
│   ├── constants.ts            # 配置常量
│   └── package.json            # Node.js依赖
├── src/                        # ROS2功能包
│   ├── slam/                   # SLAM包
│   │   ├── scripts/
│   │   │   ├── occupancy_grid_mapper.py    # 栅格建图
│   │   │   ├── simple_odometry.py          # 里程计估算
│   │   │   └── interactive_goal_setter.py  # 手动目标设置
│   │   ├── launch/
│   │   │   └── slam_with_planning.launch.py # 系统启动器
│   │   └── config/
│   │       └── slam_params.yaml            # SLAM参数
│   ├── path_planning/          # 路径规划包
│   │   ├── scripts/
│   │   │   ├── astar_planner.py           # A*算法
│   │   │   ├── pure_pursuit_controller.py # 路径跟随器
│   │   │   ├── goal_pose_bridge.py        # 目标接口
│   │   │   └── path_planning_service.py   # 规划服务
│   │   ├── srv/
│   │   │   └── PlanPath.srv               # 服务定义
│   │   └── config/
│   │       └── path_planning_params.yaml  # 规划参数
│   └── mqtt_bridge/            # MQTT集成
│       ├── scripts/
│       │   └── mqtt_ros_bridge.py         # MQTT-ROS桥接
│       └── config/
│           └── mqtt_config.yaml           # MQTT设置
├── configs/                    # 系统配置
│   └── robot.yaml             # 机器人参数
├── scripts/                    # 工具脚本
│   ├── start_full_system.sh   # 自动启动器
│   ├── stop_full_system.sh    # 系统关闭
│   ├── start_rviz_slam.sh     # RViz启动器
│   ├── start_slam_demo.sh     # SLAM演示
│   ├── kill_all_ros.sh        # 紧急清理
│   └── verify_gazebo_setup.sh # 系统验证
└── docs/                      # 文档
    ├── presentation_outline.md # 演示材料
    └── proposal.md            # 项目提案
```

## ⚙️ 配置参数

### SLAM参数 (`src/slam/config/slam_params.yaml`)
```yaml
occupancy_grid_mapper:
  ros__parameters:
    map_width: 200              # 栅格数量
    map_height: 200             # 栅格数量
    map_resolution: 0.1         # 米/栅格
    publish_rate: 2.0           # Hz
    hit_probability: 0.7        # 占用栅格概率
    miss_probability: 0.3       # 自由栅格概率
    max_range: 10.0            # 最大激光范围
```

### 路径规划参数 (`src/path_planning/config/path_planning_params.yaml`)
```yaml
astar_planner:
  ros__parameters:
    inflation_radius: 0.4       # 障碍物膨胀半径 (米)
    robot_radius: 0.2          # 机器人半径 (米)
    allow_diagonal: true       # 启用对角线移动
    heuristic_weight: 1.0      # A*启发式权重

pure_pursuit_controller:
  ros__parameters:
    lookahead_distance: 1.2    # 前瞻距离 (米)
    max_linear_velocity: 1.5   # 最大线速度 (m/s)
    max_angular_velocity: 3.0  # 最大角速度 (rad/s)
    goal_tolerance: 0.1        # 目标容忍度 (米)
```

### 仪表盘配置 (`dashboard/constants.ts`)
```typescript
export const MAP_SIZE = 40;           // 地图栅格大小
export const MAP_RESOLUTION = 0.5;    // 米/像素
export const UPDATE_RATE_MS = 50;     // 20Hz更新频率
export const LIDAR_RAYS = 360;        // LiDAR分辨率
export const LIDAR_MAX_RANGE = 10.0;  // 最大范围
```

## 🛠️ 开发指南

### 添加新功能

1. **ROS2节点**: 在 `src/[package]/scripts/` 中添加新节点
2. **Web组件**: 在 `dashboard/components/` 中创建新的React组件
3. **MQTT话题**: 在MQTT桥接配置中定义新话题
4. **启动文件**: 更新 `src/[package]/launch/` 中的启动文件

### 测试

```bash
# 测试ROS2包
colcon test --packages-select slam path_planning

# 测试Web仪表盘
cd dashboard
npm test

# 集成测试
./scripts/run_integration_tests.sh
```

### 生产构建

```bash
# 构建ROS2工作空间
colcon build --cmake-args -DCMAKE_BUILD_TYPE=Release

# 构建仪表盘
cd dashboard
npm run build

# 创建部署包
./scripts/create_deployment.sh
```

## 📖 相关文档

- **[English Documentation](README.md)**: 本README的英文版本
- **[API文档](docs/api.md)**: ROS2话题和服务
- **[用户指南](docs/user_guide.md)**: 逐步使用说明

## 🤝 贡献指南

1. Fork 仓库
2. 创建功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 打开 Pull Request

## 📄 开源许可

本项目采用MIT许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 🙏 致谢

- **陶武**: 项目开发和维护
- **ROS2社区**: 框架和工具
- **TurtleBot3**: 机器人平台和仿真
- **React/TypeScript**: Web界面技术

---

<div align="center">
<p>使用 ❤️ 通过 ROS2、React 和现代Web技术构建</p>

**🌟 如果觉得有用请给个Star!**
</div>